{
  "name": "later-gator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^22.18.0 || >=24.11.0"
  },
  "description": "Single-tenant Raindrop organization and retrieval Worker",
  "scripts": {
    "audit:production": "npm audit --omit=dev --audit-level=high",
    "build": "wrangler deploy --dry-run --outdir dist",
    "check": "npm run types:check && npm run lint && npm run test",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types",
    "types:check": "wrangler types --check && npm run typecheck"
  },
  "cloudflare": {
    "bindings": {
      "STATE": {
        "description": "Stores small, versioned lifecycle and operational documents. It never stores bookmark content."
      },
      "AI": {
        "description": "Runs organization inference when Cloudflare Workers AI is selected."
      },
      "ORGANIZE_QUEUE": {
        "description": "Carries bookmark IDs only and invokes one sequential organization job at a time."
      },
      "EMAIL": {
        "description": "Sends intervention-required alerts after the owner configures a Cloudflare Email Sending domain and verifies the chosen destination."
      },
      "INSTALLATION_SECRET": {
        "description": "Choose one private password containing at least 10 characters. You will use it to open Later Gator setup; Later Gator generates the machine connection secret automatically."
      }
    }
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.19.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.8",
    "@eslint/js": "^10.0.1",
    "@vitest/coverage-v8": "^4.1.10",
    "eslint": "^10.7.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.65.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.114.0"
  }
}
