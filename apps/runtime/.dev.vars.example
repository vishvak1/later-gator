# Per-installation credential-encryption key. Provisioning generates 32 random
# bytes and stores the base64 value as a Worker secret; never reuse it between
# installations or commit a populated value.
INSTANCE_MASTER_KEY=

# MCP and capture credentials are generated by Later Gator. OpenAI and
# Anthropic credentials are entered later in Settings.
