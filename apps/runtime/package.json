{
  "name": "@later-gator/runtime",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Later Gator personal data-plane runtime",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "check": "npm run build:web && npm run types:check && npm run typecheck:web && npm run test && npm run test:web",
    "deploy": "npm run db:init:remote && wrangler deploy",
    "dev": "wrangler dev",
    "db:init:local": "wrangler d1 execute DB --local --file schema.sql",
    "db:init:remote": "wrangler d1 execute DB --remote --file schema.sql",
    "test": "vitest run",
    "test:web": "vitest run --config vitest.web.config.ts",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types && npm run types:r2",
    "types:r2": "wrangler types worker-configuration-r2.d.ts --config wrangler.thumbnail-r2-types.jsonc --env-file wrangler.types.vars --env-interface R2ThumbnailBindingEnv --include-runtime false",
    "types:r2:check": "wrangler types worker-configuration-r2.d.ts --config wrangler.thumbnail-r2-types.jsonc --env-file wrangler.types.vars --env-interface R2ThumbnailBindingEnv --include-runtime false --check",
    "types:check": "wrangler types --check && npm run types:r2:check && npm run typecheck",
    "build:web": "node ../../scripts/build-web.mjs",
    "predev": "npm run build:web",
    "prebuild": "npm run build:web",
    "typecheck:web": "tsc -p web/tsconfig.json --noEmit"
  },
  "dependencies": {
    "@later-gator/contracts": "0.1.0",
    "@cloudflare/puppeteer": "^1.3.0",
    "@cloudflare/workers-oauth-provider": "^0.10.3",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.19.0",
    "zod": "^4.4.3"
  }
}
