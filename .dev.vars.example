# Later Gator password: choose at least 10 characters. Keep this field blank in
# the template so every installation must choose its own value.
BOOTSTRAP_PASSWORD=

# MCP and capture credentials are generated by Later Gator. OpenAI and
# Anthropic credentials are entered later in Settings.
